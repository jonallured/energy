<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
     <!-- Customize your theme here. -->
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
  </style>

  <style name="LightTheme" parent="AppTheme">
    <item name="launchImage">@drawable/launch_screen_light</item>
    <item name="backgroundColor">@color/splashscreen_bg_light</item>
    <item name="android:windowLightNavigationBar">true</item>
  </style>

  <style name="DarkTheme" parent="AppTheme">
    <item name="launchImage">@drawable/launch_screen_dark</item>
    <item name="backgroundColor">@color/splashscreen_bg_dark</item>
    <item name="android:windowLightNavigationBar">false</item>
  </style>

  <style name="SplashScreenTheme" parent="AppTheme">
    <item name="android:navigationBarColor">@color/splashscreen_bg_light</item>
  </style>

</resources>
